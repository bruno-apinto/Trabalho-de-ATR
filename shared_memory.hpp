struct MemoriaCompartilhada {
    bool i_encoder;
    int i_lidar;

    bool o_liga_camera;
    int o_aceleracao;

    bool e_inspecao;
    bool e_automatico;

    bool c_automatico;
    bool c_man;
    int j_sp_velocidade;

    bool falha; //variável que identifica uma falha
};